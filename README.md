ПП ДЗ 1:

Проверял вычисления на нескольких функциях:
  1) y = x
  2) y = sin(x)
  3) y = e^(-x^2)
  4) y = sin(x) * cos(x*2)

<img width="384" height="163" alt="image" src="https://github.com/user-attachments/assets/01d3640d-ca0b-47a7-b260-ecc3d71fa59d" />

Анализ результатов:
1) При вычислении любой функции "Монитор" оказался сам медленным. 
2) Последовательное вычисление оказался самым быстрым для 1 функции, но в остальных проигрывал параллельному (кроме "Монитора").
3) Параллельное вычисление с локальным аккумулятором оказался незначительно быстрее, чем параллельное вычисление через доступ к аккумулятору через "Atomic".

В данной версии кода вычисляется функция указаная под номером "4", при проверке иных функций менял на другие в функции function.
